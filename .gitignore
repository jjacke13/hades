# build
/build/
/result
/result-*

# direnv
/.direnv/

# editor / tooling
/.cache/
compile_commands.json
/.superpowers/

# runtime
/session.log
/.hades/
# secrets you `source` (HADES_API_KEY / TELEGRAM_BOT_TOKEN) — never commit
.env
